# must be unique in a given SonarQube instance
sonar.projectKey=toni-final-project
# --- optional properties ---
# defaults to project key
sonar.projectName=toni-final-project
# defaults to 'not provided'
#sonar.projectVersion=1.0
# Path is relative to the sonar-project.properties file. Defaults to ".". Better if we put src/
sonar.sources=src
sonar.coverage.exclusions=.test.tsx, .test.ts, .modules.tsx, src/firebase/firebase.ts, src/index.tsx # Path to test reports (it´s always the same) and expected test file config
sonar.javascript.lcov.reportPaths=coverage/lcov.info
# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8
#----- Default SonarQube server
sonar.host.url=http://localhost:9000/
sonar.login=4443b152f9e020d8522272ac25b04ba85b63b4e2